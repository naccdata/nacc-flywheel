---
name: CI gear/base

on:
  pull_request:
    paths:
      - 'requirements.txt'
      - 'python-default.lock'
      - '.github/workflows/python_reusable.yaml'
      - '.github/workflows/ci_pull_directory.yaml'
      - 'gear/pull_directory/**'
  workflow_dispatch:  # Allows to trigger the workflow manually in GitHub UI

jobs:
  ci-gear-{PACKAGE_NAME}:
    uses:
      ./.github/workflows/python_reusable.yml
    with:
      working-directory: gear/pull_directory
    secrets: inherit"""
    )