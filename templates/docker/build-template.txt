file(name="manifest", source="manifest.json")

docker_image(name="${IMAGENAME}",
             source="Dockerfile",
             dependencies=[":manifest", "gear/${DIRNAME}/src/python/${APPNAME}:bin"],
             image_tags=["${IMAGETAG}", "latest"])
